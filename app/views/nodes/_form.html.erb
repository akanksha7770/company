<%= form_for @nodes do |f| %>
 
  <% if @nodes.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@nodes.errors.count, "error") %> prohibited
        this nodes from being saved:
      </h2>
      <ul>
        <% @nodes.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :node_name %><br>
    <%= f.text_field :node_name %>
  </p>

  <p>
    <%= f.label :place_id %><br>
    <%= f.text_field :place_id %>
  </p>
 
 
 
  <p>
    <%= f.submit %>
  </p>
 
<% end %>